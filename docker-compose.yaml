version: '3.7'

networks:
  app-net:

services:
  app:
    build:
      context: .
      dockerfile: .docker/dockerfile
    image: coppel-search-service_app
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
    volumes:
      - .:/app
    env_file:
      - .env
    ports:
      - 8001:8001
    networks:
      - app-net
